#include<stdio.h>
#define MAX 10000
int main()
{
	int price[MAX];
	int sale[MAX];
	int down;
	int n=0;
	int gov_price,gov_index;
	scanf("%d%d%d",&gov_price,&price[n],&sale[n]);
	n++;
	while(true){
		scanf("%d%d",&price[n],&sale[n]);
		if(price[n]==-1&&sale[n]==-1)break;
		int p=price[n],s=sale[n];
		while()
	}
	return 0;
}
